apiVersion: v1
kind: Namespace
metadata:
  name: nudgebee

---

apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: nudgebee-node-agent
  name: nudgebee-node-agent
  namespace: nudgebee
spec:
  selector:
    matchLabels:
      app: nudgebee-node-agent
  template:
    metadata:
      labels:
        app: nudgebee-node-agent
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '80'
    spec:
      tolerations:
        - operator: Exists
      hostPID: true
      containers:
        - name: nudgebee-node-agent
          image: ghcr.io/nudgebee/node-agent:latest
          imagePullPolicy: IfNotPresent
          args: ["--cgroupfs-root", "/host/sys/fs/cgroup"]
          ports:
            - containerPort: 80
              name: http
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /host/sys/fs/cgroup
              name: cgroupfs
              readOnly: true
            - mountPath: /sys/kernel/debug
              name: debugfs
              readOnly: false
      volumes:
        - hostPath:
            path: /sys/fs/cgroup
          name: cgroupfs
        - hostPath:
            path: /sys/kernel/debug
          name: debugfs
